# Python-specific .gitignore
# 
# Note: Most ignore rules are defined in the root .gitignore file.
# This file is kept for python-specific overrides only.
#
# If you need to add python-specific ignore patterns, add them here.
# Otherwise, add patterns to the root .gitignore file.

# Python-specific environment files
.env

# test report specific ignore list
tests/reports/*
!tests/reports/generate_manifest.py

